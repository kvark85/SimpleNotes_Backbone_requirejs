# SimpleNotes_Backbone_requirejs
Simple web aplication with RESTful, GulpJS, Bower, RequireJS (AMD with optimization), backbonejs, Bootstrap

How to install:
npm install
bower install
(npm install -g bower)